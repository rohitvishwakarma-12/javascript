// ARRAY
const myArr =[0,1,2,3,4,5]
const myHeros =["shaktiman","naagraj"]

const myArr2 = new Array(1,2,3,4)

// console.log(myHeros[1]);

// Array nethods 

// myArr.push(2) // push is use to insert the number or element at the last of the array like the stack
// myArr.push(7)
// myArr.pop() // pop is used to remove the last element or the numver from the array like stack

// console.log(myArr);

// myArr.unshift(9) // unshift use for fix the number at the starting of the array

// myArr.shift()  // shift use for removing the number from the starting of the array

// console.log(myArr.includes(9)); // include use to check the number present in the array
// console.log(myArr.indexOf(3));

// const newArray = myArr.join() // join use to join two array and convert it into string

// console.log(myArr);

// console.log(newArray);

// slice, splice

// console.log("A", myArr);

// const myn1 = (myArr.slice(1,3))

// console.log(myn1);
console.log("B", myArr);

const myn2 = myArr.splice(1,3)
console.log("c",myArr)
console.log(myn2);







